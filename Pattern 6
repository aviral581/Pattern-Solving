#include <iostream>
using namespace std;

int main()
{
    int n;
    cout<<"enter the value"<<endl;
    cin>>n;
    for(int r=1;r<=n;r++){
        for(int c=1;c<=r;c++){
            char ch='A'+r-1;
            cout<<ch;
        }
        cout<<endl;
    }

    return 0;
}
